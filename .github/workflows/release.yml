name: Build and Release Local Docker Image

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release Tag Name (e.g. v0.1.2)'
        required: true
        default: 'v0.0.1'

jobs:
  release:
    name: Build, Package and Release SpideGo
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set Release Tag
        id: get_tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "TAG_NAME=${{ github.event.inputs.tag_name }}" >> $GITHUB_ENV
          else
            echo "TAG_NAME=${{ github.ref_name }}" >> $GITHUB_ENV
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase Repository Name for GHCR
        id: repo_names
        run: |
          echo "IMAGE_NAME=ghcr.io/$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Build Docker Image
        run: |
          docker build -f Dockerfile.local \
            -t spidego:latest \
            -t spidego:${{ env.TAG_NAME }} .

      - name: Save and Split Docker Image to Compressed Tarball
        run: |
          echo "Packaging and splitting SpideGo Docker image..."
          docker save spidego:latest | gzip | split -b 1500M - spidego-local-${{ env.TAG_NAME }}.tar.gz.part-
          echo "Image packaged and split successfully:"
          ls -lh spidego-local-${{ env.TAG_NAME }}.tar.gz.part-*

      - name: Tag and Push Docker Image to GHCR
        run: |
          docker tag spidego:latest ${{ env.IMAGE_NAME }}:latest
          docker tag spidego:latest ${{ env.IMAGE_NAME }}:${{ env.TAG_NAME }}
          docker push ${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.IMAGE_NAME }}:${{ env.TAG_NAME }}

      - name: Upload Tarball Parts to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG_NAME }}
          name: SpideGo ${{ env.TAG_NAME }}
          draft: false
          prerelease: false
          files: spidego-local-${{ env.TAG_NAME }}.tar.gz.part-*
          body: |
            ## SpideGo - Local Search Engine

            This release contains the complete local image.

            ### Services & Ports:
            - **Frontend UI**: `http://localhost:8890`
            - **Backend API & Docs**: `http://localhost:8889/docs`
            - **SearXNG Engine**: `http://localhost:8888`

            ---

            ### 📦 Option 1: Run with Downloaded Release Tarball (Offline / Local)
            1. Download all `spidego-local-${{ env.TAG_NAME }}.tar.gz.part-*` files attached below into the same directory.
            2. Recombine the parts into a single archive:
               - **Linux / macOS**:
                 ```bash
                 cat spidego-local-${{ env.TAG_NAME }}.tar.gz.part-* > spidego-local-${{ env.TAG_NAME }}.tar.gz
                 ```
               - **Windows (Command Prompt)**:
                 ```cmd
                 copy /b spidego-local-${{ env.TAG_NAME }}.tar.gz.part-* spidego-local-${{ env.TAG_NAME }}.tar.gz
                 ```
               - **Windows (PowerShell)**:
                 ```powershell
                 Get-Content spidego-local-${{ env.TAG_NAME }}.tar.gz.part-* -ReadCount 0 | Set-Content spidego-local-${{ env.TAG_NAME }}.tar.gz -Encoding Byte
                 ```
            3. Load the image into Docker:
               ```bash
               docker load -i spidego-local-${{ env.TAG_NAME }}.tar.gz
               ```
            4. Run the container:
               ```bash
               docker run -d \
                 --name spidego \
                 -p 8888:8888 \
                 -p 8889:8889 \
                 -p 8890:8890 \
                 --restart unless-stopped \
                 spidego:latest
               ```

            ---

            ### 🐳 Option 2: Run with Docker Compose (GHCR)
            Create a `docker-compose.yml`:
            ```yaml
            services:
              spidego:
                image: ${{ env.IMAGE_NAME }}:latest
                container_name: spidego
                ports:
                  - "8888:8888" # SearXNG
                  - "8889:8889" # Backend FastAPI
                  - "8890:8890" # Next.js Frontend
                restart: unless-stopped
            ```
            Run:
            ```bash
            docker compose up -d
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}